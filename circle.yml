database:
  override:
    - bundle exec rake db:setup

machine:
  services:
    - docker

deployment:
  hub:
    branch: master
    commands:
      - ./scripts/build_image.sh

test:
  pre:
    - cp dotenv .env
